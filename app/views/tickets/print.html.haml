
%div{class:"row"}
  %div{class:"small-9 large-centered columns"}
    %h2 Tickets

= render 'generate_form'
%div{class:"row"}
  %div{class:"small-12 columns text-center"}
    %p{class:"tickets-columns"}
      - Ticket.all.each do |ticket|
        - if !ticket.taken?
          #{ticket.token} - Einlösen auf
          %br
          https://ms-erstiwe.herokuapp.com
          %br

%div{class:"row"}
  %div{class:"small-12 columns text-center"}
    %h3 Eingelöste Tickets:
    %p{class:"tickets-columns"}
      - Ticket.taken.each do |ticket|
        #{ticket.token}
        - if ticket.user.withdrawn?
          = ": Zurückgetreten"

